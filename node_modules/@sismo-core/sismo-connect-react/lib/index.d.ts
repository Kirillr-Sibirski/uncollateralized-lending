import React from 'react';
import { ClaimRequest, AuthRequest, SignatureRequest, SismoConnectResponse, SismoConnectConfig, SismoConnectClient } from '@sismo-core/sismo-connect-client';
export * from '@sismo-core/sismo-connect-client';

type ButtonProps = {
    claim?: ClaimRequest;
    claims?: ClaimRequest[];
    auth?: AuthRequest;
    auths?: AuthRequest[];
    signature?: SignatureRequest;
    onResponse?: (response: SismoConnectResponse) => void;
    onResponseBytes?: (responseBytes: string) => void;
    config: SismoConnectConfig;
    callbackPath?: string;
    callbackUrl?: string;
    namespace?: string;
    verifying?: boolean;
    loading?: boolean;
    text?: string;
    overrideStyle?: React.CSSProperties;
    disabled?: boolean;
    theme?: "light" | "dark" | "black";
};
declare const SismoConnectButton: ({ claims, claim, auths, auth, signature, onResponse, onResponseBytes, config, callbackPath, callbackUrl, namespace, verifying, text, loading, overrideStyle, disabled, theme, }: ButtonProps) => JSX.Element;

type SismoConnectHook = {
    response: SismoConnectResponse | null;
    responseBytes: string;
    sismoConnect: SismoConnectClient;
};
type SismoConnectProps = {
    config: SismoConnectConfig;
};
declare const useSismoConnect: ({ config }: SismoConnectProps) => SismoConnectHook;

export { SismoConnectButton, SismoConnectHook, SismoConnectProps, useSismoConnect };
