import { SismoConnectClient, SismoConnectConfig, SismoConnectResponse } from "@sismo-core/sismo-connect-client";
export type SismoConnectHook = {
    response: SismoConnectResponse | null;
    responseBytes: string;
    sismoConnect: SismoConnectClient;
};
export type SismoConnectProps = {
    config: SismoConnectConfig;
};
export declare const useSismoConnect: ({ config }: SismoConnectProps) => SismoConnectHook;
