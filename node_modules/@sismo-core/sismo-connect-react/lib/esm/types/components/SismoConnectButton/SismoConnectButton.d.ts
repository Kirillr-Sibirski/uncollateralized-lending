import React from "react";
import "./SismoConnectButton.css";
import { SismoConnectResponse, SismoConnectConfig, ClaimRequest, AuthRequest, SignatureRequest } from "@sismo-core/sismo-connect-client";
type ButtonProps = {
    claim?: ClaimRequest;
    claims?: ClaimRequest[];
    auth?: AuthRequest;
    auths?: AuthRequest[];
    signature?: SignatureRequest;
    onResponse?: (response: SismoConnectResponse) => void;
    onResponseBytes?: (responseBytes: string) => void;
    config: SismoConnectConfig;
    callbackPath?: string;
    callbackUrl?: string;
    namespace?: string;
    verifying?: boolean;
    loading?: boolean;
    text?: string;
    overrideStyle?: React.CSSProperties;
    disabled?: boolean;
    theme?: "light" | "dark" | "black";
};
export declare const SismoConnectButton: ({ claims, claim, auths, auth, signature, onResponse, onResponseBytes, config, callbackPath, callbackUrl, namespace, verifying, text, loading, overrideStyle, disabled, theme, }: ButtonProps) => JSX.Element;
export {};
